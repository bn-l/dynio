# yaml-language-server: $schema=./cmd-config-schema.json

where:
    command: where
    description: Searches files by filename. Use * as a wildcard. E.g. *test*.
    arguments:
        - /t
    hotkeyNumber: 1
    placeholderText: Search files
    noOutputTimeoutMs: 100
    modeConfig:
        mode: list
        displayOptions:
            parseAnsiColors: true
            maxLineLength: 160
            lineSplitter: "\n"
            fontSize: 0.8
        activationOptions:
            activateAction: copy